# 2026-04-15 20:00 - AutomaticForm Phrase Selector

## Overview
Implementação de um seletor de frases e ícones no formulário de Auto DJ (`AutomaticForm.svelte`). O recurso permite associar até 7 frases de transição com ícones pré-definidos, utilizando um novo componente de Modal privado e dados centralizados.

---

### 1. Novo Componente Privado

#### `Modal.svelte`
- Criado em `resources/js/ui/components/private` para separar a interface administrativa da pública.
- Design alinhado ao padrão do painel (cabeçalho `blue-skywave`, texto em negrito/itálico).
- Exportado no arquivo `index.js` da pasta para facilitar o uso em outros widgets.

---

### 2. Refatoração do `AutomaticForm.svelte`

#### Gerenciamento de Estado
- Refatoração completa para utilizar o hook `useForm` do Inertia, padronizando com o restante do projeto.
- Inicialização do array `phrases` com 7 objetos vazios `{ image: null, phrase: "" }`.
- Implementação da busca de dados (fetch) para o modo de edição via `identifier`.

#### Interface de Usuário (UI)
- Adição de uma seção dedicada "Frases e Ícones" com layout otimizado.
- Uso de componentes `Preview` para a imagem principal e botões de seleção para as frases de transição.
- Estilização do cabeçalho da seção utilizando o padrão visual de separadores com linhas horizontais.

---

### 3. Integração com Dados Centralizados

#### Ícones de Locução
- Migração da lista de ícones que estava "hardcoded" para a importação de `locutionIcons` de `@/data`.
- O seletor (Modal) utiliza essa lista para renderizar uma grade de ícones selecionáveis.
- A função de seleção armazena o caminho do arquivo (`url`) no estado do formulário.

---

*Generated by Antigravity AI*
