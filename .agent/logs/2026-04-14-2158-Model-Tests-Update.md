# 2026-04-14 21:58 - Model Tests Update

## Overview
Revisão completa dos testes unitários de models em `tests/Unit/Models`. Foram identificadas divergências entre o estado atual dos models e os arquivos de teste existentes. Os testes foram corrigidos (quando errados) e novos arquivos foram criados para os models sem cobertura.

---

### 1. Correções em Testes Existentes

#### `MusicTest.php`
- **Problema**: O namespace estava incorreto — `Tests\Unit` ao invés de `Tests\Unit\Models`.
- **Correção**: Atualizado o namespace para `Tests\Unit\Models`.

#### `CalendarTest.php`
- **Problema**: O único teste de scope chamava `Calendar::active()->get()`, mas o model `Calendar` não possui `scopeActive`. O scope existente é `scopeValid`, que usa `TIMESTAMP(date, hour) > NOW()` (MySQL).
- **Correção**: O teste foi reescrito como `testValidScope`, chamando `Calendar::valid()`. Como o scope usa a função `NOW()` exclusiva do MySQL, foi adicionado `markTestSkipped` para ambientes SQLite (padrão nos testes automatizados).

#### `AutomaticTest.php`
- **Problema**: O model `Automatic` possui `scopeActive`, mas não havia nenhum teste cobrindo esse scope.
- **Correção**: Adicionado `testActiveScope` seguindo o padrão dos outros testes.

#### `RoleTest.php`
- **Problema**: O model `Role` possui a relação `members()` (belongsToMany com `User`), mas não havia teste para ela. Também faltava o import de `User`.
- **Correção**: Adicionado `testMembersRelationship` e o import de `App\Models\User`.

---

### 2. Novos Arquivos de Teste Criados

Os seguintes models existiam mas não possuíam arquivo de teste:

| Arquivo criado | Model coberto | O que testa |
|---|---|---|
| `PermissionTest.php` | `Permission` | `testRolesRelationship` — belongsToMany com `Role` via `permissions_pivot` |
| `PlaylistBattleTest.php` | `PlaylistBattle` | `testDayCast` — cast de `day` para inteiro |

---

### 3. Expansão de Testes para Cobrir a Relação Inversa

Os models `PollOption`, `PostCategory`, `PostReaction` e `PostReference` possuem relação `belongsTo` de volta ao model pai, mas não tinham essa direção testada. Em vez de criar novos arquivos (o que seria redundante), as asserções foram adicionadas dentro dos testes já existentes:

#### `PollTest.php` — `testOptionsRelationship`
- Adicionado: `$poll->options->first()->poll->is($poll)`

#### `PostTest.php` — `testReferencesRelationship`, `testReactionsRelationship`, `testCategoriesRelationship`
- Adicionado em cada um: `->first()->post->is($post)` para verificar o caminho inverso

---

### 3. Resultado dos Testes

```
Tests:    1 skipped, 68 passed (100 assertions)
Duration: 1.68s
```

- **68 testes passando** (100% dos executáveis)
- **1 skipped**: `CalendarTest > valid scope` — ignorado intencionalmente em SQLite pois o scope usa funções exclusivas do MySQL (`TIMESTAMP()` e `NOW()`).

---

*Generated by Antigravity AI*
