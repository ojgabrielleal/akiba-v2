# 2026-04-15 20:15 - Automatic Route Registration

## Overview
Registro das rotas para as operações de Auto DJ no controlador `AdministrationController` e ajuste das URLs no frontend para garantir a comunicação correta entre os componentes e o servidor.

---

### 1. Rotas do Servidor

#### `web.php`
- Adição do grupo de rotas `automatic` dentro do prefixo `administration`.
- Rotas registradas:
    - `POST` /panel/administration/automatic -> `createAutomatic`
    - `PATCH` /panel/administration/automatic/{automatic:uuid} -> `updateAutomatic`
    - `GET` /panel/administration/automatic/{automatic:uuid} -> `showAutomatic`
    - `DELETE` /panel/administration/automatic/{automatic:uuid} -> `deactivateAutomatic`
- As rotas foram inseridas respeitando a organização existente e mantendo as rotas de `task` intactas.

---

### 2. Ajustes no Frontend

#### `AutomaticForm.svelte`
- Atualização das URLs de busca (`axios.get`) e de submissão do formulário para utilizarem o novo prefixo `/panel/administration/automatic/`.

#### `AutomaticGrid.svelte`
- Implementação da função `requestDeactivateProgram`, que utiliza o `router.delete` do Inertia para desativar um Auto DJ após confirmação do usuário.

---

*Generated by Antigravity AI*
